name: Real AERMET smoke

# Exercises pyaermod against a real AERMET binary. Downloads the EPA
# AERMET v24142 Fortran source, compiles with gfortran, and runs
# tests/test_real_aermet.py. Parallel to real_aermod.yml /
# real_aermap.yml.

on:
  schedule:
    - cron: "0 8 * * 1"  # Monday 08:00 UTC (after AERMOD + AERMAP weekly)
  workflow_dispatch:
    inputs:
      source_url:
        description: "EPA SCRAM AERMET source archive URL (override to try a new EPA release)"
        required: false
        default: "https://gaftp.epa.gov/Air/aqmg/SCRAM/models/met/aermet/aermet_source.zip"
  pull_request:
    paths:
      - "src/pyaermod/aermet.py"
      - "src/pyaermod/aermet_runner.py"
      - "src/pyaermod/met_ingest.py"
      - "src/pyaermod/met_qaqc.py"
      - "tests/test_real_aermet.py"
      - ".github/workflows/real_aermet.yml"

env:
  # `inputs.source_url` is only set for workflow_dispatch; every other
  # trigger falls through to the pinned default.
  AERMET_SOURCE_URL: ${{ inputs.source_url || 'https://gaftp.epa.gov/Air/aqmg/SCRAM/models/met/aermet/aermet_source.zip' }}

jobs:
  aermet_smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install pyaermod
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Compute EPA source cache key
        id: src
        # Keyed on the URL (so an override never reuses the default's archive)
        # plus the calendar month, so a same-URL EPA re-release is picked up
        # within a month while gaftp flakiness inside the month is absorbed.
        run: |
          echo "key=epa-aermet-source-$(printf '%s' "$AERMET_SOURCE_URL" | sha256sum | cut -c1-16)-$(date -u +%Y-%m)" >> "$GITHUB_OUTPUT"

      - name: Cache EPA source archive
        uses: actions/cache@v4
        with:
          path: /tmp/aermet_build/aermet_source.zip
          key: ${{ steps.src.outputs.key }}

      - name: Download + compile AERMET from EPA source
        run: |
          set -euxo pipefail
          sudo apt-get update -qq && sudo apt-get install -y -qq gfortran
          mkdir -p /tmp/aermet_build && cd /tmp/aermet_build
          # fetch_epa_source.sh reuses a cache-restored archive, prints the
          # archive's top-level dir (EPA's version) and adds it to the job summary.
          bash "$GITHUB_WORKSPACE/scripts/fetch_epa_source.sh" \
            "$AERMET_SOURCE_URL" \
            aermet_source.zip
          unzip -q aermet_source.zip
          # EPA archives extract either into a versioned subdir (aermod/aermap)
          # or flat at the root (aermet). cd into the subdir only if there is one.
          topdir=$(unzip -Z1 aermet_source.zip | awk -F/ 'NF>1{print $1; exit}')
          [ -n "$topdir" ] && cd "$topdir"
          # Guard against read-only extraction (as seen with EPA's AERMOD v26135).
          chmod -R u+w .
          # AERMET v24142 is Fortran 2008 free-form; modules compile first
          gfortran -c -O2 -std=f2008 -ffree-form mod_file_units.f90
          gfortran -c -O2 -std=f2008 -ffree-form mod_main1.f90
          gfortran -c -O2 -std=f2008 -ffree-form mod_upperair.f90
          gfortran -c -O2 -std=f2008 -ffree-form mod_surface.f90
          gfortran -c -O2 -std=f2008 -ffree-form mod_onsite.f90
          gfortran -c -O2 -std=f2008 -ffree-form mod_pbl.f90
          gfortran -c -O2 -std=f2008 -ffree-form mod_read_input.f90
          gfortran -c -O2 -std=f2008 -ffree-form mod_reports.f90
          gfortran -c -O2 -std=f2008 -ffree-form mod_misc.f90
          gfortran -c -O2 -std=f2008 -ffree-form aermet.f90
          gfortran -O2 -o aermet *.o
          chmod +x aermet
          sudo cp aermet /usr/local/bin/aermet
          aermet --help || true

      - name: Run real-AERMET smoke test
        run: pytest tests/test_real_aermet.py -v
